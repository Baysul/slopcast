{
  "name": "@slopcast/native-livekit",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check": "cargo fmt --check && cargo clippy --all-targets -- -D warnings",
    "rust:fmt": "cargo fmt",
    "rust:check": "cargo fmt --check"
  }
}
