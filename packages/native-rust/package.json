{
  "name": "@screen-share/native-rust",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "prepare": "napi build --platform --release",
    "dev": "napi build --platform",
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform",
    "rust:fmt": "cargo fmt",
    "rust:check": "cargo fmt --check"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.7.2"
  }
}
